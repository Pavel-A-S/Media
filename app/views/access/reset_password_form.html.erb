<%= form_for(:access, url: create_reset_link_path) do |f| %>

  <%= f.label :email, t(:email), class: "label" %>
  <%= f.email_field :email, class: "text_field" %>

  <%= f.submit t(:send_reset_link), class: "button_submit" %>
<% end %>
